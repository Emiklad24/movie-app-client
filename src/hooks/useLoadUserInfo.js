// import { useQuery } from "react-query";
// import { loadUser } from "../services/auth.service";
// import { authStore } from "../store/auth.store";
// import { loadUserInfoKey } from "../util/appCacheKeys";

export const useLoadUserInfo = () => {
  // const loadUserToStore = authStore((state) => state?.loadUser);
  // const resetAuth = authStore((state) => state?.resetAuth);
  // const { data, isLoading, isError } = useQuery(loadUserInfoKey, loadUser, {
  //   onSuccess: (returnedData) => {
  //     // loadUserToStore(returnedData);
  //   },
  //   onError: () => {
  //     resetAuth();
  //   },
  // });
  // return {
  //   isLoading,
  //   data,
  //   isError,
  // };
};
